# react/api-controller@6142

TanStack Query hooks that keep HTTP access outside pages and components.

- Change identifiers, imports, endpoints, contracts, and query or mutation options.
- Preserve typed HTTP calls, centralized query keys, and targeted cache invalidation.
- Keep rendering, form state, navigation, and page-specific feedback outside API controllers.
