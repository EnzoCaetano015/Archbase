# react/component@5217

Reusable React component with typed props and optional colocated styles and entrypoint.

- Change identifiers, imports, props, rendered content, accessibility labels, and styles.
- Preserve typed props, named exports, presentation responsibility, and theme-token styling.
- Keep page-specific orchestration and API calls outside shared components.
- Retain optional files only when they improve reuse or readability.
