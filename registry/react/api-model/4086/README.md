# react/api-model@4086

Typed API contracts and stable query keys for one frontend resource.

- Change identifiers, fields, request and response shapes, and query keys.
- Preserve explicit request/response types and centralized immutable query keys.
- Keep requests, cache operations, rendering, and page behavior outside model files.
