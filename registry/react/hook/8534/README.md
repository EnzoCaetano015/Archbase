# react/hook@8534

Shared React hook that owns one reusable stateful behavior.

- Change identifiers, types, state, and callbacks.
- Preserve a focused responsibility, named export, stable callbacks, and explicit return shape.
- Keep page-specific orchestration in the page hook rather than promoting it prematurely.
