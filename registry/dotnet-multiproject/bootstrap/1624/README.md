# dotnet-multiproject/bootstrap@1624

ASP.NET Core composition root for a multi-project solution.

- Delegate dependency groups to focused registration extensions.
- Preserve middleware order, especially exception handling, authentication, authorization, and endpoint mapping.
- Keep feature workflows out of `Program.cs`.
