# dotnet-multiproject/repository@6485

Repository boundary for PetaPoco and SQLKata persistence.

- Expose persistence operations through an interface consumed by the application project.
- Build parameterized queries and map results to shared contract models.
- Keep application workflows and HTTP behavior outside the infrastructure project.
