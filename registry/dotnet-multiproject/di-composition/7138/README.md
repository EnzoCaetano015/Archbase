# dotnet-multiproject/di-composition@7138

Composition extension connecting project-layer interfaces and implementations.

- Register application and infrastructure dependencies in one API-owned composition boundary.
- Choose lifetimes explicitly and validate required configuration during startup.
- Use assembly scanning only when missing or duplicate implementations fail visibly.
