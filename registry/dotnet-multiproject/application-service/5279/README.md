# dotnet-multiproject/application-service@5279

Application service contract and implementation for a multi-project solution.

- Keep the public interface beside other application contracts.
- Implement workflows against repository interfaces and shared models.
- Do not reference controllers, HTTP results, SQL builders, or concrete database clients.
