# nestjs/service@4793

Injectable application service for a feature module.

- Coordinate use cases through constructor-injected dependencies.
- Raise framework exceptions only when the service intentionally owns application error mapping.
- Do not depend on controllers, Express response objects, or route decorators.
