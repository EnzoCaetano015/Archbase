# nestjs/bootstrap@1437

Single NestJS application entrypoint for global transport concerns.

- Register validation, filters, interceptors, CORS, cookies, and API documentation centrally.
- Keep feature providers and workflows out of the bootstrap file.
- Read deployment-specific values from configuration instead of embedding secrets.
