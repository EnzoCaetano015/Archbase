# nestjs/controller@2864

Thin HTTP adapter for one NestJS feature.

- Express routes and transport input with Nest decorators and DTOs.
- Delegate application behavior to an injected service.
- Keep database queries and external API orchestration outside controllers.
