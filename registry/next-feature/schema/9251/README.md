# next-feature/schema@9251

Runtime contract for input that crosses a feature boundary.

- Parse untrusted values in actions and route handlers before invoking application services.
- Infer TypeScript types from the runtime schema instead of maintaining duplicate contracts.
- Keep I/O and presentation concerns outside schema modules.
