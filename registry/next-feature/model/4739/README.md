# next-feature/model@4739

Framework-independent model for one feature.

- Define stable domain types, constants, and pure transformations.
- Keep I/O, React, Next.js, validation libraries, and database clients outside this layer.
- Prefer colocated tests for non-trivial policy and transformations.
