# next-feature/component@5086

Feature UI bundle that makes the React Server Component boundary explicit.

- Render data in a Server Component by default.
- Isolate state, transitions, events, and browser APIs in the smallest practical `"use client"` component.
- Pass serializable values across the server-to-client boundary and delegate mutations to Server Actions.
