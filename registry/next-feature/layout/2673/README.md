# next-feature/layout@2673

Shared App Router layout for one route segment.

- Compose navigation, providers, and visual shells around `children`.
- Keep the default server boundary and add a client boundary only around interactive providers.
- Do not move feature-specific reads, mutations, or workflows into a layout.
