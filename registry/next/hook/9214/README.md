# next/hook@9214

Custom React hook with an explicit result type and named export.

- Change identifiers, imports, types, and state logic.
- Preserve the public result shape before the hook and keep state orchestration inside the hook.
