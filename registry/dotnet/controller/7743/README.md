# dotnet/controller@7743

ASP.NET Core controller with routing attributes, constructor injection, cancellation, and service delegation.

- Change identifiers, namespaces, types, routes, and action logic.
- Preserve attribute placement, dependency injection, delegation, and cancellation propagation.
