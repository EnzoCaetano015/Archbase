# dotnet/repository@5532

Repository interface that exposes asynchronous persistence operations with cancellation.

- Change identifiers, namespaces, entity types, and query methods.
- Preserve the interface boundary, task-based operations, and cancellation propagation.
