# dotnet/service@1172

Application service with constructor injection, repository delegation, and cancellation propagation.

- Change identifiers, namespaces, types, and application logic.
- Preserve the sealed service, dependency boundary, repository delegation, and cancellation token.
