# react-tailwind/api-controller@7251

TanStack Query hooks that use a configured Axios client, centralized endpoint catalog, and direct backend contracts.

- Change identifiers, imports, contracts, and query or mutation options.
- Preserve endpoint-catalog usage, typed payloads, stable query keys, and query/mutation separation.
- Keep literal endpoint paths, rendering, and page-specific form orchestration outside controllers.
