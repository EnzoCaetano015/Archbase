# react-tailwind/page@6592

React page bundle using semantic Tailwind tokens, shared UI primitives, a colocated orchestration hook, optional Zod validation, and optional pure helpers.

- Change identifiers, imports, content, orchestration, validation, classes, and pure helpers.
- Preserve thin rendering, primitive reuse, and API access through controllers.
- Avoid effects for state that can be derived while rendering.
- Keep schema and utility files only when the page needs those responsibilities.
