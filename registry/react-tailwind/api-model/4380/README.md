# react-tailwind/api-model@4380

Typed request and response contracts that mirror direct backend payloads and expose stable query-key identities.

- Change identifiers, fields, request/response shapes, and query keys to match the real API.
- Preserve contract fidelity, request/response separation, and centralized query-key identities.
- Do not add UI-only fields or perform requests from model files.
