# react-tailwind/component@5463

Typed domain component composed from reusable UI primitives and semantic Tailwind tokens.

- Change identifiers, imports, props, content, semantic classes, and primitive composition.
- Preserve typed props, semantic HTML, named exports, and reuse of the UI primitive layer.
- Keep page-specific orchestration out of shared components; add hook or utility files only when they own a real responsibility.
