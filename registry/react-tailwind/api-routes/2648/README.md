# react-tailwind/api-routes@2648

Immutable catalog that centralizes backend paths for API controllers.

- Change identifiers, endpoint groups, and paths.
- Preserve one transport-only catalog and literal types through `as const`.
- Do not place HTTP calls, query keys, UI behavior, or environment-specific base URLs here.
