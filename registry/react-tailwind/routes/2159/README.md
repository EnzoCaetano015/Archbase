# react-tailwind/routes@2159

React Router bundle that separates page imports and registration from the declarative navigation tree.

- Change identifiers, imports, registered pages, paths, layouts, redirects, and route elements.
- Preserve one page registry, one route tree, declarative redirects, and the router-provider boundary.
- Do not introduce a second routing system or move remote data access into route configuration.
