# spring-boot/mapper@6583

Compile-time mapper between transport contracts and persistence entities.

- Use the Spring component model for constructor injection into services.
- Ignore generated identifiers and lifecycle-managed fields explicitly.
- Keep database calls and application workflows outside mapping declarations.
