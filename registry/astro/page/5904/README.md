# astro/page@5904

File-routed Astro page that queries validated content during the build and composes it through a shared document layout.

- Change identifiers, imports, metadata, collection query, rendered content, and semantic classes.
- Preserve file-based routing, server frontmatter, layout composition, optimized assets, and build-time content access.
- Ship no client JavaScript by default; hydrate a framework component only when interaction requires it.
