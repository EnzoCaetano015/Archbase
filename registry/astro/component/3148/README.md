# astro/component@3148

Typed Astro component with server frontmatter, semantic markup, and slot-based composition.

- Change identifiers, imports, props, content, semantic classes, and slots.
- Preserve typed props and the frontmatter/markup responsibility boundary.
- Keep the component server-rendered by default; add browser scripts or hydrated framework islands only when interaction requires them.
