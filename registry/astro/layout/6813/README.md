# astro/layout@6813

Astro document layout that owns metadata, global styles, shared shell components, and the page slot.

- Change identifiers, imports, metadata, shell components, and document language.
- Preserve a complete HTML document, typed metadata props, one global-style boundary, and a slot for page content.
- Keep route-specific content and collection queries in pages rather than layouts.
